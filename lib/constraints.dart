import 'package:flutter/material.dart';

const primaryColor = Color(0xFFF2656ca);

var upcomingCardColor = Colors.white;

const startingColor = Color(0xFF37d5d6);

const endingColor = Color(0xFF36096d);

const appBarTitleTextStyle = TextStyle(
  fontSize: 18,
  letterSpacing: 0.9,
  color: Colors.white,
);

const tabBarTitleTextStyle =
    TextStyle(fontSize: 14, letterSpacing: 0.9, color: Colors.white);

const runTextStyle = TextStyle(
    fontSize: 14,
    letterSpacing: 0.8,
    fontWeight: FontWeight.normal,
    color: endingColor);
const subtitleTextStyle =
    TextStyle(fontSize: 10, letterSpacing: 0.8, color: Colors.grey);

const subtitleTextStyle2 =
    TextStyle(fontSize: 10, letterSpacing: 0.8, color: Colors.white);

const specialTitleTextStyle =
    TextStyle(color: Colors.deepPurple, fontWeight: FontWeight.bold,letterSpacing: 0.8);
